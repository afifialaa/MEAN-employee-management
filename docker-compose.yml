version: '3.7'

services:

  mongo-db:
    image: mongo
    ports:
      - '27017:27017'

  server:
    build: ./server
    image: afifi/mean
    ports:
      - '8080:8080'
    links:
      - mongo-db

  client:
    build: client
    image: afif/mean-front
    ports:
        - '4200:4200'